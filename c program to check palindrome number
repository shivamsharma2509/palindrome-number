#include<stdio.h>
int main()
{
  int n,r,rev=0;
  printf(enter any number:-);
  scanf(%d,&n);
  
  while(n!=0)
  {
    r=n%10;
    rev=rev*10+r;
    n/=10;
  }
if(rev==n)
 {
  printf(palindome number);
 }
else
  printf(not a palindrome number);
}
